{
  "name": "admin-portal-backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  },
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "engines": {
    "node": "20.x"
  }
}
